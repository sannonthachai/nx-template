export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum Language {
  TH = 'th',
  EN = 'en',
  VN = 'vn',
}

export enum Country {
  VN = 'vn',
  TH = 'th',
}

export enum EnabledVnMasterPass {
  ENABLED = '1',
  DISABLED = '0',
}

export enum Roles {
  STUDENT = 'student',
  COACH = 'coach',
  ADMIN = 'admin',
}

export enum AdminStatus {
  DEACTIVATE = 0,
  ACTIVATE = 1,
}
